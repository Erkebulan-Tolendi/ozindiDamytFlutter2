import 'package:flutter/material.dart';

class MarathonePage extends StatelessWidget {
  const MarathonePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Marathone Page"),
      ),
      body: Center(
        child: Text("Marathone Page"),
      ),
    );
  }
}

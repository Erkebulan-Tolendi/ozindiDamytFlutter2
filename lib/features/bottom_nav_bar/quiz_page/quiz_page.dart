import 'package:flutter/material.dart';

class QuizzPageBot extends StatelessWidget {
  const QuizzPageBot({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Quizz pAge "),
      ),
      body: Center(
        child: Text("quizPage"),
      ),
    );
  }
}
